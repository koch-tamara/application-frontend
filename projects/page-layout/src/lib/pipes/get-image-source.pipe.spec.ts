import { GetImageSourcePipe } from './get-image-source.pipe';

describe('GetImageSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new GetImageSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
